# Copy to .env.local for sync during local dev. These are the client-side
# Supabase URL + publishable key — safe to expose (row-level security protects
# data; the vault ciphertext is end-to-end encrypted before upload).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
